"""Punto de entrada. Si ves imports a api_client, alguien revirtió el archivo.

Fallback: python -m src.free_digest_app --run-once
"""

from src.free_digest_app import main

if __name__ == "__main__":
    main()
